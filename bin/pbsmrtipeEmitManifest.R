#!/usr/bin/env Rscript

# Write Static MetaTasks Manifests to json
#library("pbsmrtpipeR")
args <- commandArgs(trailingOnly = TRUE)

main <- function() {
    cat("Starting main\n")
    cat(args)
    cat("Exiting main\n")
}

main()
